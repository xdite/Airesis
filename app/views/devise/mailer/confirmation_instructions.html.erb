<% @host = Setting.domain %>

<% @url = user_confirmation_url(:confirmation_token => @resource.confirmation_token, :host => @host, l: @resource.original_locale.lang) %>

<p><%= t('devise.mailer.confirmation_instructions.welcome', {app_name: Setting.app_short_name , user: @resource.fullname}) %></p>

<p><%= t('devise.mailer.confirmation_instructions.follow_link') %></p>

<p><%= link_to t('devise.mailer.confirmation_instructions.confirm_account'), @url %></p>

<p><%= t('devise.mailer.confirmation_instructions.follow_link_2') %></p>

<p><%= link_to @url, @url %></p>

<p><%= t('devise.mailer.confirmation_instructions.access_by_email', {app_name: Setting.app_short_name}) %></p>

<p><%= t('devise.mailer.confirmation_instructions.contact_assistance', {mail: Setting.app_email_address }) %></p>